# 페이지 기획서 (PAGES.md)

> 각 페이지의 목적, 포함 컴포넌트, UI 요소, 사용자 흐름을 정의합니다.

---

## 전체 페이지 구조

```
/                   → Landing (홈)
/login              → 로그인 / 회원가입
/onboarding         → 온보딩 (최초 1회)
/dashboard          → 대시보드 (메인)
/checklist/:category → 카테고리별 체크리스트
/chat               → AI 챗봇
/resources          → 참고 자료
/archive            → 선배 후기 (Optional)
/profile            → 내 프로필 / 설정
```

---

## 1. Landing Page (`/`)

**목적**: 서비스 소개, 신규 유저 유입

**주요 요소**
- 히어로 섹션: 서비스 한 줄 설명 + "Start My SSAP" CTA 버튼
- 기능 소개 섹션: 체크리스트 / AI 챗봇 / 짐 목록 생성기 카드 3개
- SSAP 프로그램 간략 소개
- 푸터: KENTECH 로고, 팀 정보

**사용자 흐름**
- 비로그인 상태에서 접근 가능
- CTA 클릭 → `/login` 이동

---

## 2. 로그인 / 회원가입 (`/login`)

**목적**: KENTECH 이메일 기반 인증

**주요 요소**
- 이메일 입력 필드 (`@kentech.ac.kr` 도메인 검증)
- 비밀번호 입력 필드
- 로그인 / 회원가입 탭 전환
- Supabase Auth 연동

**사용자 흐름**
- 신규 유저 → 회원가입 후 `/onboarding`
- 기존 유저 → 로그인 후 `/dashboard`

---

## 3. 온보딩 (`/onboarding`)

**목적**: 개인화 체크리스트 생성을 위한 정보 수집 (최초 1회)

**주요 요소**
- Step 1: 목적지 선택 (호스트 대학 드롭다운 — MVP: UCLA만)
- Step 2: 출발일 입력 (Date Picker)
- Step 3: 체류 기간 입력 (주 단위)
- Step 4: SSAP 경험 여부 (첫 참가 / 재참가)
- 진행률 표시 바 (Step 1/4 형태)

**사용자 흐름**
- 완료 → AI가 체크리스트 자동 생성 → `/dashboard`

---

## 4. 대시보드 (`/dashboard`)

**목적**: 전체 준비 현황 한눈에 파악

**주요 요소**
- 상단: 유저 이름, 목적지, D-day 카운터
- 전체 진행률 바 (예: 67% 완료)
- 카테고리 카드 6개
  - 비자, 항공권, 숙소, 보험, eSIM, 짐 꾸리기
  - 각 카드: 완료 항목 수, 마감일 가장 임박한 항목 표시
- 우측 사이드: 다가오는 마감일 목록 (D-30 이내)
- AI 추천 다음 할 일 배너

**컴포넌트 목록**
- `<DdayCounter />` — 출발까지 남은 날
- `<ProgressBar />` — 전체 진행률
- `<CategoryCard />` — 카테고리별 카드
- `<DeadlineList />` — 임박 마감일
- `<AiRecommendBanner />` — AI 추천 태스크

---

## 5. 체크리스트 상세 (`/checklist/:category`)

**category 값**: `visa` / `flights` / `accommodation` / `insurance` / `esim` / `packing`

**목적**: 카테고리별 세부 항목 확인 및 체크

**공통 요소**
- 카테고리 제목 + 진행률 바
- 항목 리스트 (체크박스 + 항목명 + 권장 완료 기한)
- 각 항목 클릭 시 상세 정보 펼치기 (accordion)
  - 설명, 필요 서류, 공식 링크

**비자 (`/checklist/visa`) 전용 요소**
- DS-160 작성 가이드 링크
- 대사관 예약 시스템 바로가기
- F-1 비자 필요 서류 목록

**짐 꾸리기 (`/checklist/packing`) 전용 요소**
- AI 짐 목록 생성기 버튼
- 카테고리별 분류 (의류, 전자기기, 서류, 의약품)

---

## 6. AI 챗봇 (`/chat`)

**목적**: 언제든지 질문할 수 있는 AI 어시스턴트

**주요 요소**
- 채팅 UI (말풍선 형태)
- 입력창 + 전송 버튼
- 추천 질문 버튼 (예: "DS-160이 뭐야?", "비자 인터뷰 준비 방법")
- 대화 기록 (세션 내 유지)

**참고**: 사이드 독 형태로 `/dashboard` 및 `/checklist` 페이지에서도 접근 가능

---

## 7. 참고 자료 (`/resources`)

**목적**: 국가별 공식 사이트 및 양식 모음

**주요 요소**
- 목적지별 탭 (MVP: 미국/UCLA만)
- 링크 카드: 대사관, 항공사, 보험사, 공식 양식 다운로드

---

## 8. 선배 후기 아카이브 (`/archive`) — Optional

**목적**: SSAP 선배들의 학교별 팁 공유

**주요 요소**
- 학교별 필터
- 후기 카드 (작성자, 연도, 학교, 내용 요약)
- AI 요약 배지 (가장 유용한 팁 상위 노출)
- 후기 작성 버튼 (이수자만 가능)

---

## 9. 내 프로필 (`/profile`)

**목적**: 개인 설정 및 계정 관리

**주요 요소**
- 목적지 / 출발일 수정
- 알림 설정 (이메일)
- 진행 현황 백업 / 초기화
- 로그아웃
